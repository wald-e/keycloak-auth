{
    "authenticators": [
        {
            "name": "check_group",
            "fileName": "check_group.js",
            "description": "Deny login if user has no group membership"
        }
    ]
}
